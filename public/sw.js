var __wpo = {
  "assets": {
    "main": [
      "./fonts/arapey-regular-webfont.eot?7d292a37c6b1e2a0d517f86c8fc26d4f",
      "./fonts/badscript-regular-webfont.eot?5667bf00ee0241b099f385a69ea3ec76",
      "./fonts/billabong-webfont.eot?258b028fac31571dc3e34920fdd80e47",
      "./fonts/fontawesome-webfont.eot?674f50d287a8c48dc19ba404d20fe713",
      "./fonts/handlee-regular-webfont.eot?a371bdae58e081c1ebadb1de680457a5",
      "./fonts/philosopher-regular-webfont.eot?d9d15ee9c1ac286c9fba8146ab07727e",
      "./fonts/poiretone-regular-webfont.eot?43baf21f815c5a4192287f9125c00b32",
      "./fonts/raleway-regular-webfont.eot?081e7947f0d167bbe737917b049781b7",
      "./fonts/spirax-regular-webfont.eot?099eb048277949975e4685f7e126bc92",
      "./fonts/arapey-regular-webfont.svg?32dd6aaef91d0c58f5bdd5a882266ee4",
      "./fonts/arapey-regular-webfont.ttf?aa4ce403971e1acdb3ef95ec4249b53d",
      "./fonts/arapey-regular-webfont.woff?63977363d16b4a47c49342a4f21c7030",
      "./fonts/arapey-regular-webfont.woff2?2c0102a6f7763abb20548ab1160df44e",
      "./fonts/badscript-regular-webfont.svg?b89bf16ef34b477652a70481fc588e59",
      "./fonts/badscript-regular-webfont.ttf?5c6b08ce2d53936fb1d1f1fd7905595d",
      "./fonts/badscript-regular-webfont.woff?9c016e6929fd1fb5f8815ce64209cd09",
      "./fonts/badscript-regular-webfont.woff2?4d1ad12f0bde3f97b4ce6b96477eea12",
      "./fonts/billabong-webfont.svg?9e4a3fdbbc03442c7844d542e8a05ba4",
      "./fonts/billabong-webfont.ttf?9e027aa9f30326cdd794fa25446f0ac2",
      "./fonts/billabong-webfont.woff?65ee16278d11cf98e01efdf0b58d78d8",
      "./fonts/fontawesome-webfont.svg?acf3dcb7ff752b5296ca23ba2c7c2606",
      "./fonts/fontawesome-webfont.ttf?b06871f281fee6b241d60582ae9369b9",
      "./fonts/fontawesome-webfont.woff?fee66e712a8a08eef5805a46892932ad",
      "./fonts/fontawesome-webfont.woff2?af7ae505a9eed503f8b8e6982036873e",
      "./fonts/handlee-regular-webfont.svg?cea5fbc94a34b6b68dc4f4fe974a2131",
      "./fonts/handlee-regular-webfont.ttf?f9a127eab8c96c746241dc43c67a0dba",
      "./fonts/handlee-regular-webfont.woff?082ca7798d8231c0b7b1821ffd91b4dc",
      "./fonts/handlee-regular-webfont.woff2?580464cd5b2b50b04c32fe2919c67e87",
      "./fonts/philosopher-regular-webfont.svg?02063c4149a1beb2837a61eb5096ca7c",
      "./fonts/philosopher-regular-webfont.ttf?bb3b107dcdeba8ff8ef87b7a829564c0",
      "./fonts/philosopher-regular-webfont.woff?07aac63993cb7136c5d817a3deaaf07f",
      "./fonts/philosopher-regular-webfont.woff2?9ea01c4f52baf517ee8963b626a7407b",
      "./fonts/poiretone-regular-webfont.svg?176a051a8d8deb02763e5887905a3efc",
      "./fonts/poiretone-regular-webfont.ttf?e18b178ee0f1c7473e34c071517aa681",
      "./fonts/poiretone-regular-webfont.woff?64f3af78f4f44c26f3e48be42022bf88",
      "./fonts/poiretone-regular-webfont.woff2?36439a631c6a22943ec412bc0e6e523a",
      "./fonts/raleway-regular-webfont.svg?48ecaa49289c9b1ed02b6e226c9dd2c0",
      "./fonts/raleway-regular-webfont.ttf?9dea09f27e294f1035b475cad12e83a6",
      "./fonts/raleway-regular-webfont.woff?13aba690203ccf1836fcbaca34f1a1a0",
      "./fonts/raleway-regular-webfont.woff2?c11ab7dd84940fe81dca8a748e07a817",
      "./fonts/spirax-regular-webfont.svg?1d9864ea4682fcd700ac4bc8f1cb804d",
      "./fonts/spirax-regular-webfont.ttf?f03e317c9f05c73fae3a2b27a1e6fb61",
      "./fonts/spirax-regular-webfont.woff?66f5a5c0c558e61dd9307b0d6b67a0f5",
      "./fonts/spirax-regular-webfont.woff2?100f27b1f2a7fc8739d1240777c9b9a7",
      "/js/app.js",
      "/css/app.css",
      "./mix-manifest.json"
    ],
    "additional": [],
    "optional": []
  },
  "externals": [],
  "hashesMap": {
    "90945df71e3546b225908c3d79c8efa199c4adb5": "./fonts/arapey-regular-webfont.eot?7d292a37c6b1e2a0d517f86c8fc26d4f",
    "3bf1944e47093f41d36a453aff2e44d21f362d29": "./fonts/badscript-regular-webfont.eot?5667bf00ee0241b099f385a69ea3ec76",
    "4bbb822a24dbec6d59a7ac078b7b7cf228303a9a": "./fonts/billabong-webfont.eot?258b028fac31571dc3e34920fdd80e47",
    "d980c2ce873dc43af460d4d572d441304499f400": "./fonts/fontawesome-webfont.eot?674f50d287a8c48dc19ba404d20fe713",
    "32b726dc7e6cffc87c3f0f187344de0d2879c3ab": "./fonts/handlee-regular-webfont.eot?a371bdae58e081c1ebadb1de680457a5",
    "cc4597e6d308c1a416d997a58cd8223ee41925a1": "./fonts/philosopher-regular-webfont.eot?d9d15ee9c1ac286c9fba8146ab07727e",
    "458bbbdc0f0a8b6539d4f2ac2af491d90d75f39a": "./fonts/poiretone-regular-webfont.eot?43baf21f815c5a4192287f9125c00b32",
    "1213287ba393d552f5449d3b89bc93d9a51685ca": "./fonts/raleway-regular-webfont.eot?081e7947f0d167bbe737917b049781b7",
    "4c0c995dd0f2144a5089a4326aea8d0c97d05eaa": "./fonts/spirax-regular-webfont.eot?099eb048277949975e4685f7e126bc92",
    "4415333d40d786c1cd1feb7cc3ba9537405849c9": "./fonts/arapey-regular-webfont.svg?32dd6aaef91d0c58f5bdd5a882266ee4",
    "e8d254f6c000dda9107d61816f766883e5fbac0d": "./fonts/arapey-regular-webfont.ttf?aa4ce403971e1acdb3ef95ec4249b53d",
    "b9dac391ccb0e374fbb065f36451bb1b1e238e87": "./fonts/arapey-regular-webfont.woff?63977363d16b4a47c49342a4f21c7030",
    "1b6daa9ddef337a2496f1463aa12f331645e4bc3": "./fonts/arapey-regular-webfont.woff2?2c0102a6f7763abb20548ab1160df44e",
    "75a981f7a90ffe6e28e30bb07a9bd2356129c903": "./fonts/badscript-regular-webfont.svg?b89bf16ef34b477652a70481fc588e59",
    "069787895bc3d24008b87b594dfab718dbe54d53": "./fonts/badscript-regular-webfont.ttf?5c6b08ce2d53936fb1d1f1fd7905595d",
    "bb34d883994d757130803430e011f2ffaa011fe8": "./fonts/badscript-regular-webfont.woff?9c016e6929fd1fb5f8815ce64209cd09",
    "b28eaefc0d43f8e6bf23523128dc389e3c226b46": "./fonts/badscript-regular-webfont.woff2?4d1ad12f0bde3f97b4ce6b96477eea12",
    "5bf9a93c9e960f546cf373feff97582504b3f527": "./fonts/billabong-webfont.svg?9e4a3fdbbc03442c7844d542e8a05ba4",
    "1813a21f9f42cc8c124a665ea989366374345bdf": "./fonts/billabong-webfont.ttf?9e027aa9f30326cdd794fa25446f0ac2",
    "e0df9ef08444fcd944c8b80c2ab8a910b6b60e4d": "./fonts/billabong-webfont.woff?65ee16278d11cf98e01efdf0b58d78d8",
    "b5483b11f8ba213e733b5b8af9927a04fec996f6": "./fonts/fontawesome-webfont.svg?acf3dcb7ff752b5296ca23ba2c7c2606",
    "13b1eab65a983c7a73bc7997c479d66943f7c6cb": "./fonts/fontawesome-webfont.ttf?b06871f281fee6b241d60582ae9369b9",
    "28b782240b3e76db824e12c02754a9731a167527": "./fonts/fontawesome-webfont.woff?fee66e712a8a08eef5805a46892932ad",
    "d6f48cba7d076fb6f2fd6ba993a75b9dc1ecbf0c": "./fonts/fontawesome-webfont.woff2?af7ae505a9eed503f8b8e6982036873e",
    "00ea32dbce7f675166dbfd6f054117722abbeb42": "./fonts/handlee-regular-webfont.svg?cea5fbc94a34b6b68dc4f4fe974a2131",
    "77921517109bd656c7730e3cd9500eeef5e89e83": "./fonts/handlee-regular-webfont.ttf?f9a127eab8c96c746241dc43c67a0dba",
    "709aefafad3966aef769f18bcf02dcf29f68181a": "./fonts/handlee-regular-webfont.woff?082ca7798d8231c0b7b1821ffd91b4dc",
    "e8b6e3f5a8d501aa149ecfd544f6ec80759f39e5": "./fonts/handlee-regular-webfont.woff2?580464cd5b2b50b04c32fe2919c67e87",
    "5158537f979fdfee6219bd1658031fb84289d2b0": "./fonts/philosopher-regular-webfont.svg?02063c4149a1beb2837a61eb5096ca7c",
    "fac60cd3073d54034d8059a2da1344a338b2fa04": "./fonts/philosopher-regular-webfont.ttf?bb3b107dcdeba8ff8ef87b7a829564c0",
    "9a9fc81adebfc590da7332af5fad7aaff2ff68eb": "./fonts/philosopher-regular-webfont.woff?07aac63993cb7136c5d817a3deaaf07f",
    "f75c1b149bacdc452c6750e63d6207a4980dcb6b": "./fonts/philosopher-regular-webfont.woff2?9ea01c4f52baf517ee8963b626a7407b",
    "ecb2a1c862dcfc6ec18989bea862f500ae13241c": "./fonts/poiretone-regular-webfont.svg?176a051a8d8deb02763e5887905a3efc",
    "25cf44253c71b1a4659d9c864bf59dda17c3ac20": "./fonts/poiretone-regular-webfont.ttf?e18b178ee0f1c7473e34c071517aa681",
    "9a70cb0a2e65a84914f7516a4bbdba605a7d27d8": "./fonts/poiretone-regular-webfont.woff?64f3af78f4f44c26f3e48be42022bf88",
    "0db5603d11e544eb57fcc53af8c35aaad35e5191": "./fonts/poiretone-regular-webfont.woff2?36439a631c6a22943ec412bc0e6e523a",
    "79e2751e5053010ed74e0067f7a5f4671bcc0c7f": "./fonts/raleway-regular-webfont.svg?48ecaa49289c9b1ed02b6e226c9dd2c0",
    "d8b7f5c17d41359d0bca587ea769052eb3d4fc97": "./fonts/raleway-regular-webfont.ttf?9dea09f27e294f1035b475cad12e83a6",
    "4f8b672f71e1e10a7c33b2819c10d87279b183b3": "./fonts/raleway-regular-webfont.woff?13aba690203ccf1836fcbaca34f1a1a0",
    "0e72ef844d41753ac786c664fe2827a92a7f6bc5": "./fonts/raleway-regular-webfont.woff2?c11ab7dd84940fe81dca8a748e07a817",
    "fe9f86513eb4085b0b851e9637f7031e2448e525": "./fonts/spirax-regular-webfont.svg?1d9864ea4682fcd700ac4bc8f1cb804d",
    "b57e5f150ea7407abd60a472d30ff5aa5147f53b": "./fonts/spirax-regular-webfont.ttf?f03e317c9f05c73fae3a2b27a1e6fb61",
    "da2270f6a58eadddb7ea6403d7a355aa5011a3f3": "./fonts/spirax-regular-webfont.woff?66f5a5c0c558e61dd9307b0d6b67a0f5",
    "84bd9a256e3904e8091a26dd77b78e7e437bbb30": "./fonts/spirax-regular-webfont.woff2?100f27b1f2a7fc8739d1240777c9b9a7",
    "32fef4687d4650f14308dc9da3302056e6deca3c": "/js/app.js",
    "4719be444265bc4cb3ceddfdacf7f194b642b650": "/css/app.css",
    "d01fb14f2a5db581361b4785abac31bde3855ede": "./mix-manifest.json"
  },
  "strategy": "changed",
  "responseStrategy": "cache-first",
  "version": "4/28/2017, 12:46:27 PM",
  "name": "webpack-offline",
  "pluginVersion": "4.6.2",
  "relativePaths": true
};

/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// identity function for calling harmony imports with the correct context
/******/ 	__webpack_require__.i = function(value) { return value; };
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {



/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

      

if (typeof DEBUG === 'undefined') {
  var DEBUG = false;
}

function WebpackServiceWorker(params, helpers) {
  var loaders = helpers.loaders;
  var cacheMaps = helpers.cacheMaps;

  var strategy = params.strategy;
  var responseStrategy = params.responseStrategy;

  var assets = params.assets;
  var loadersMap = params.loaders || {};

  var hashesMap = params.hashesMap;
  var externals = params.externals;

  // Not used yet
  // const alwaysRevalidate = params.alwaysRevalidate;
  // const ignoreSearch = params.ignoreSearch;
  // const preferOnline = params.preferOnline;

  var CACHE_PREFIX = params.name;
  var CACHE_TAG = params.version;
  var CACHE_NAME = CACHE_PREFIX + ':' + CACHE_TAG;

  var STORED_DATA_KEY = '__offline_webpack__data';

  mapAssets();

  var allAssets = [].concat(assets.main, assets.additional, assets.optional);
  var navigateFallbackURL = params.navigateFallbackURL;

  self.addEventListener('install', function (event) {
    console.log('[SW]:', 'Install event');

    var installing = undefined;

    if (strategy === 'changed') {
      installing = cacheChanged('main');
    } else {
      installing = cacheAssets('main');
    }

    event.waitUntil(installing);
  });

  self.addEventListener('activate', function (event) {
    console.log('[SW]:', 'Activate event');

    var activation = cacheAdditional();

    // Delete all assets which name starts with CACHE_PREFIX and
    // is not current cache (CACHE_NAME)
    activation = activation.then(storeCacheData);
    activation = activation.then(deleteObsolete);
    activation = activation.then(function () {
      if (self.clients && self.clients.claim) {
        return self.clients.claim();
      }
    });

    event.waitUntil(activation);
  });

  function cacheAdditional() {
    if (!assets.additional.length) {
      return Promise.resolve();
    }

    if (DEBUG) {
      console.log('[SW]:', 'Caching additional');
    }

    var operation = undefined;

    if (strategy === 'changed') {
      operation = cacheChanged('additional');
    } else {
      operation = cacheAssets('additional');
    }

    // Ignore fail of `additional` cache section
    return operation['catch'](function (e) {
      console.error('[SW]:', 'Cache section `additional` failed to load');
    });
  }

  function cacheAssets(section) {
    var batch = assets[section];

    return caches.open(CACHE_NAME).then(function (cache) {
      return addAllNormalized(cache, batch, {
        bust: params.version,
        request: params.prefetchRequest
      });
    }).then(function () {
      logGroup('Cached assets: ' + section, batch);
    })['catch'](function (e) {
      console.error(e);
      throw e;
    });
  }

  function cacheChanged(section) {
    return getLastCache().then(function (args) {
      if (!args) {
        return cacheAssets(section);
      }

      var lastCache = args[0];
      var lastKeys = args[1];
      var lastData = args[2];

      var lastMap = lastData.hashmap;
      var lastVersion = lastData.version;

      if (!lastData.hashmap || lastVersion === params.version) {
        return cacheAssets(section);
      }

      var lastHashedAssets = Object.keys(lastMap).map(function (hash) {
        return lastMap[hash];
      });

      var lastUrls = lastKeys.map(function (req) {
        var url = new URL(req.url);
        url.search = '';

        return url.toString();
      });

      var sectionAssets = assets[section];
      var moved = [];
      var changed = sectionAssets.filter(function (url) {
        if (lastUrls.indexOf(url) === -1 || lastHashedAssets.indexOf(url) === -1) {
          return true;
        }

        return false;
      });

      Object.keys(hashesMap).forEach(function (hash) {
        var asset = hashesMap[hash];

        // Return if not in sectionAssets or in changed or moved array
        if (sectionAssets.indexOf(asset) === -1 || changed.indexOf(asset) !== -1 || moved.indexOf(asset) !== -1) return;

        var lastAsset = lastMap[hash];

        if (lastAsset && lastUrls.indexOf(lastAsset) !== -1) {
          moved.push([lastAsset, asset]);
        } else {
          changed.push(asset);
        }
      });

      logGroup('Changed assets: ' + section, changed);
      logGroup('Moved assets: ' + section, moved);

      var movedResponses = Promise.all(moved.map(function (pair) {
        return lastCache.match(pair[0]).then(function (response) {
          return [pair[1], response];
        });
      }));

      return caches.open(CACHE_NAME).then(function (cache) {
        var move = movedResponses.then(function (responses) {
          return Promise.all(responses.map(function (pair) {
            return cache.put(pair[0], pair[1]);
          }));
        });

        return Promise.all([move, addAllNormalized(cache, changed, {
          bust: params.version,
          request: params.prefetchRequest
        })]);
      });
    });
  }

  function deleteObsolete() {
    return caches.keys().then(function (keys) {
      var all = keys.map(function (key) {
        if (key.indexOf(CACHE_PREFIX) !== 0 || key.indexOf(CACHE_NAME) === 0) return;

        console.log('[SW]:', 'Delete cache:', key);
        return caches['delete'](key);
      });

      return Promise.all(all);
    });
  }

  function getLastCache() {
    return caches.keys().then(function (keys) {
      var index = keys.length;
      var key = undefined;

      while (index--) {
        key = keys[index];

        if (key.indexOf(CACHE_PREFIX) === 0) {
          break;
        }
      }

      if (!key) return;

      var cache = undefined;

      return caches.open(key).then(function (_cache) {
        cache = _cache;
        return _cache.match(new URL(STORED_DATA_KEY, location).toString());
      }).then(function (response) {
        if (!response) return;

        return Promise.all([cache, cache.keys(), response.json()]);
      });
    });
  }

  function storeCacheData() {
    return caches.open(CACHE_NAME).then(function (cache) {
      var data = new Response(JSON.stringify({
        version: params.version,
        hashmap: hashesMap
      }));

      return cache.put(new URL(STORED_DATA_KEY, location).toString(), data);
    });
  }

  self.addEventListener('fetch', function (event) {
    var requestUrl = event.request.url;
    var url = new URL(requestUrl);
    var urlString = undefined;

    if (externals.indexOf(requestUrl) !== -1) {
      urlString = requestUrl;
    } else {
      url.search = '';
      urlString = url.toString();
    }

    // Handle only GET requests
    var isGET = event.request.method === 'GET';
    var assetMatches = allAssets.indexOf(urlString) !== -1;
    var cacheUrl = urlString;

    if (!assetMatches) {
      var cacheRewrite = matchCacheMap(event.request);

      if (cacheRewrite) {
        cacheUrl = cacheRewrite;
        assetMatches = true;
      }
    }

    if (!assetMatches && isGET) {
      // If isn't a cached asset and is a navigation request,
      // fallback to navigateFallbackURL if available
      if (navigateFallbackURL && isNavigateRequest(event.request)) {
        event.respondWith(handleNavigateFallback(fetch(event.request)));

        return;
      }
    }

    if (!assetMatches || !isGET) {
      // Fix for https://twitter.com/wanderview/status/696819243262873600
      if (url.origin !== location.origin && navigator.userAgent.indexOf('Firefox/44.') !== -1) {
        event.respondWith(fetch(event.request));
      }

      return;
    }

    // Logic of caching / fetching is here
    // * urlString -- url to match from the CACHE_NAME
    // * event.request -- original Request to perform fetch() if necessary
    var resource = undefined;

    if (responseStrategy === "network-first") {
      resource = networkFirstResponse(event, urlString, cacheUrl);
    }
    // "cache-first"
    // (responseStrategy has been validated before)
    else {
        resource = cacheFirstResponse(event, urlString, cacheUrl);
      }

    if (navigateFallbackURL && isNavigateRequest(event.request)) {
      resource = handleNavigateFallback(resource);
    }

    event.respondWith(resource);
  });

  self.addEventListener('message', function (e) {
    var data = e.data;
    if (!data) return;

    switch (data.action) {
      case 'skipWaiting':
        {
          if (self.skipWaiting) self.skipWaiting();
        }break;
    }
  });

  function cacheFirstResponse(event, urlString, cacheUrl) {
    return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
      if (response) {
        if (DEBUG) {
          console.log('[SW]:', 'URL [' + cacheUrl + '](' + urlString + ') from cache');
        }

        return response;
      }

      // Load and cache known assets
      var fetching = fetch(event.request).then(function (response) {
        if (!response.ok) {
          if (DEBUG) {
            console.log('[SW]:', 'URL [' + urlString + '] wrong response: [' + response.status + '] ' + response.type);
          }

          return response;
        }

        if (DEBUG) {
          console.log('[SW]:', 'URL [' + urlString + '] from network');
        }

        if (cacheUrl === urlString) {
          (function () {
            var responseClone = response.clone();

            caches.open(CACHE_NAME).then(function (cache) {
              return cache.put(urlString, responseClone);
            }).then(function () {
              console.log('[SW]:', 'Cache asset: ' + urlString);
            });
          })();
        }

        return response;
      });

      return fetching;
    });
  }

  function networkFirstResponse(event, urlString, cacheUrl) {
    return fetch(event.request).then(function (response) {
      if (response.ok) {
        if (DEBUG) {
          console.log('[SW]:', 'URL [' + urlString + '] from network');
        }

        return response;
      }

      // throw to reach the code in the catch below
      throw new Error("response is not ok");
    })
    // this needs to be in a catch() and not just in the then() above
    // cause if your network is down, the fetch() will throw
    ['catch'](function () {
      if (DEBUG) {
        console.log('[SW]:', 'URL [' + urlString + '] from cache if possible');
      }

      return cachesMatch(cacheUrl, CACHE_NAME);
    });
  }

  function handleNavigateFallback(fetching) {
    return fetching['catch'](function () {}).then(function (response) {
      if (!response || !response.ok) {
        if (DEBUG) {
          console.log('[SW]:', 'Loading navigation fallback [' + navigateFallbackURL + '] from cache');
        }

        return cachesMatch(navigateFallbackURL, CACHE_NAME);
      }

      return response;
    });
  }

  function mapAssets() {
    Object.keys(assets).forEach(function (key) {
      assets[key] = assets[key].map(function (path) {
        var url = new URL(path, location);

        if (externals.indexOf(path) === -1) {
          url.search = '';
        } else {
          // Remove hash from possible passed externals
          url.hash = '';
        }

        return url.toString();
      });
    });

    Object.keys(loadersMap).forEach(function (key) {
      loadersMap[key] = loadersMap[key].map(function (path) {
        var url = new URL(path, location);

        if (externals.indexOf(path) === -1) {
          url.search = '';
        } else {
          // Remove hash from possible passed externals
          url.hash = '';
        }

        return url.toString();
      });
    });

    hashesMap = Object.keys(hashesMap).reduce(function (result, hash) {
      var url = new URL(hashesMap[hash], location);
      url.search = '';

      result[hash] = url.toString();
      return result;
    }, {});

    externals = externals.map(function (path) {
      var url = new URL(path, location);
      url.hash = '';

      return url.toString();
    });
  }

  function addAllNormalized(cache, requests, options) {
    var allowLoaders = options.allowLoaders !== false;
    var bustValue = options && options.bust;
    var requestInit = options.request || {
      credentials: 'omit',
      mode: 'cors'
    };

    return Promise.all(requests.map(function (request) {
      if (bustValue) {
        request = applyCacheBust(request, bustValue);
      }

      return fetch(request, requestInit);
    })).then(function (responses) {
      if (responses.some(function (response) {
        return !response.ok;
      })) {
        return Promise.reject(new Error('Wrong response status'));
      }

      var extracted = [];
      var addAll = responses.map(function (response, i) {
        if (allowLoaders) {
          extracted.push(extractAssetsWithLoaders(requests[i], response));
        }

        return cache.put(requests[i], response);
      });

      if (extracted.length) {
        (function () {
          var newOptions = copyObject(options);
          newOptions.allowLoaders = false;

          var waitAll = addAll;

          addAll = Promise.all(extracted).then(function (all) {
            var extractedRequests = [].concat.apply([], all);

            if (requests.length) {
              waitAll = waitAll.concat(addAllNormalized(cache, extractedRequests, newOptions));
            }

            return Promise.all(waitAll);
          });
        })();
      } else {
        addAll = Promise.all(addAll);
      }

      return addAll;
    });
  }

  function extractAssetsWithLoaders(request, response) {
    var all = Object.keys(loadersMap).map(function (key) {
      var loader = loadersMap[key];

      if (loader.indexOf(request) !== -1 && loaders[key]) {
        return loaders[key](response.clone());
      }
    }).filter(function (a) {
      return !!a;
    });

    return Promise.all(all).then(function (all) {
      return [].concat.apply([], all);
    });
  }

  function matchCacheMap(request) {
    var urlString = request.url;
    var url = new URL(urlString);

    var requestType = undefined;

    if (request.mode === 'navigate') {
      requestType = 'navigate';
    } else if (url.origin === location.origin) {
      requestType = 'same-origin';
    } else {
      requestType = 'cross-origin';
    }

    for (var i = 0; i < cacheMaps.length; i++) {
      var map = cacheMaps[i];

      if (!map) continue;
      if (map.requestTypes && map.requestTypes.indexOf(requestType) === -1) {
        continue;
      }

      var newString = undefined;

      if (typeof map.match === 'function') {
        newString = map.match(url, request);
      } else {
        newString = urlString.replace(map.match, map.to);
      }

      if (newString && newString !== urlString) {
        return newString;
      }
    }
  }
}

function cachesMatch(request, cacheName) {
  return caches.match(request, {
    cacheName: cacheName
  })
  // Return void if error happened (cache not found)
  ['catch'](function () {});
}

function applyCacheBust(asset, key) {
  var hasQuery = asset.indexOf('?') !== -1;
  return asset + (hasQuery ? '&' : '?') + '__uncache=' + encodeURIComponent(key);
}

function getClientsURLs() {
  if (!self.clients) {
    return Promise.resolve([]);
  }

  return self.clients.matchAll({
    includeUncontrolled: true
  }).then(function (clients) {
    if (!clients.length) return [];

    var result = [];

    clients.forEach(function (client) {
      var url = new URL(client.url);
      url.search = '';
      url.hash = '';
      var urlString = url.toString();

      if (!result.length || result.indexOf(urlString) === -1) {
        result.push(urlString);
      }
    });

    return result;
  });
}

function isNavigateRequest(request) {
  return request.mode === 'navigate' || request.headers.get('Upgrade-Insecure-Requests') || (request.headers.get('Accept') || '').indexOf('text/html') !== -1;
}

function copyObject(original) {
  return Object.keys(original).reduce(function (result, key) {
    result[key] = original[key];
    return result;
  }, {});
}

function logGroup(title, assets) {
  console.groupCollapsed('[SW]:', title);

  assets.forEach(function (asset) {
    console.log('Asset:', asset);
  });

  console.groupEnd();
}
      WebpackServiceWorker(__wpo, {
loaders: {},
cacheMaps: [],
});
      module.exports = __webpack_require__(0)
    

/***/ })
/******/ ]);